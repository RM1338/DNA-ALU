export default function LiveTruthTable({rows}){if(!rows?.length)return null; const inKeys=Object.keys(rows[0].inputs); const outKeys=Object.keys(rows[0].outputs); return <table className="truth mono"><thead><tr>{inKeys.map(k=><th key={k}>{k}</th>)}{outKeys.map(k=><th key={k}>{k}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i} className={Object.values(r.outputs).every(v=>v===1)?'hot':''}>{inKeys.map(k=><td key={k}>{r.inputs[k]}</td>)}{outKeys.map(k=><td key={k}>{r.outputs[k]}</td>)}</tr>)}</tbody></table>}